import type { Config } from "tailwindcss";

const config: Config = {
  // In Tailwind CSS v4, content is auto-detected and theme is defined in CSS
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};
export default config;

